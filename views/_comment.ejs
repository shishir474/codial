<div id="comment-<%= comment._id %>" style="margin-bottom:8px;"> 
    <!-- Delete option for comments -->
      <% if (locals.user && locals.user.id == comment.user.id) { %>                                                   <!--.id converts into string format-->                               
        <!-- Delete option should be visible only if the user is signed in(i.e locals.user) and the user signed in(locals.user.id) is same as the user who has created the post(post.user.id) -->
        <small>
            <a class='delete-comment-button' href="/comment/destroy/<%= comment.id %>">X</a>
        </small>
        <p>Delete link</p>

    <% } %>  
    <%= comment.content %>
    <br>
    <small>
        <%= comment.user.name%>
    </small>

    <% if (locals.user) {%>
        <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=comment" id="likebtn"> <%= comment.likes.length %> Likes</a>
    <% }else{ %>
        <small> <%= comment.likes.length %> Likes</small>
    <% } %>
    

</div>